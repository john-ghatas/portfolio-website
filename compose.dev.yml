services:
  portfolio:
    restart: always
    environment:
      - NODE_ENV=development
    build:
      target: frontend-dev
    volumes:
      - .:/usr/portfolio:ro
      - next_cache:/usr/portfolio/.next
    ports:
      - '3000:3000'
volumes:
  next_cache:
